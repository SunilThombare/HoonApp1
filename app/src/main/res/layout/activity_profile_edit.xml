<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/image_edit_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:text="사진정보 수정"
        android:onClick="onClickImageEditBtn"/>

    <Button
        android:id="@+id/basic_edit_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_edit_btn"
        android:layout_centerHorizontal="true"
        android:text="기본정보 수정"
        android:onClick="onClickBasicEditBtn"/>

    <Button
        android:id="@+id/self_introduction_edit_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/basic_edit_btn"
        android:layout_marginTop="30dp"
        android:layout_centerHorizontal="true"
        android:text="자기소개 수정"
        android:onClick="onClickSelfIntroductionEditBtn"/>

    <Button
        android:id="@+id/self_introduction_choice_edit_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/self_introduction_edit_btn"
        android:layout_centerHorizontal="true"
        android:text="자기소개(선택) 수정"
        android:onClick="onClickSelfIntroductionChoiceEditBtn">
    </Button>

</RelativeLayout>
